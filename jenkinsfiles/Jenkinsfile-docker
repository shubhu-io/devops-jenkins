pipeline {
    agent {
        docker {
            image 'node:20-alpine'
        }
    }
    stages {
        stage('Build App') {
            steps {
                sh 'npm --version'
                sh 'node --version'
            }
        }
        stage('Run Tests') {
            steps {
                sh 'echo "Unit tests passed successfully."'
            }
        }
    }
}
